# Conversor TXT para PDF em Python

Projeto simples para converter arquivos `.txt` em `.pdf` usando Python.

## Como usar

1. Instale a dependência:
```bash
pip install fpdf
```
2. Execute:
``` bash
python main.py
```
O PDF gerado será salvo na pasta arquivos/.